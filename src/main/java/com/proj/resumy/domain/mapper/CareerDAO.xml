<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.proj.resumy.domain.CareerDAO">

	<select id="select" resultType="com.proj.resumy.domain.CareerDAO">
		SELECT 
			cr_id           crId, 
			cr_company      crCompany, 
			cr_hiredate     crHiredate, 
			cr_leavedate    crLeavedate,
			cr_lvreason     crLvreason,
			cr_post         crPost,
			cr_dept         crDept,
			cr_area         crArea,
			cr_salary       crSalary,
			cr_work         crWork
			mem_id          memId
		FROM hr_spec_info
		where mem_id = #{memId}
		ORDER BY 
			cr_id DESC;
	</select>
</mapper>