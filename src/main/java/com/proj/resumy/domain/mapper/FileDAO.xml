<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.proj.resumy.file.domain.FileDAO">

	<select id="selectByMid" resultType="com.proj.resumy.file.domain.FileDTO">
		SELECT 
			file_name name, 
			file_volume volume, 
			file_regdate regdate, 
			file_memo memo 
		FROM 
			hr_file
		WHERE mem_id = #{mid}
		ORDER BY 
			file_regdate DESC;
	</select>
	
	<insert id="insert">
		INSERT INTO hr_file 
			(file_name, file_volume, file_memo, mem_id)
		VALUES 
			(#{name}, #{volume}, #{memo}, #{mid});
	</insert>
	
	<delete id="deleteById">
		DELETE FROM hr_file 
		WHERE file_id = #{id};
	</delete>
	
</mapper>