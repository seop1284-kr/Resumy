<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.proj.resumy.intro.domain.IntroDAO">
	
	<select id="selectNotFinResume" resultType="com.proj.resumy.intro.domain.IntroDTO">
		SELECT 
			intr_id "id", 
			intr_title title, 
			intr_regdate regdate, 
			intr_public pub, 
			intr_finish fin, 
			mody_dtm modydate,
			mem_id mid
		FROM 
			hr_introduction
		WHERE
			mem_id = #{mid} AND intr_finish = false
		ORDER BY 
			intr_id DESC	
	</select>
	
	<select id="selectFinResume" resultType="com.proj.resumy.intro.domain.IntroDTO">
		SELECT 
			intr_id "id", 
			intr_title title, 
			intr_regdate regdate, 
			intr_public pub, 
			intr_finish fin, 
			mody_dtm modydate,
			mem_id mid
		FROM 
			hr_introduction
		WHERE
			mem_id = #{mid} AND intr_finish = true
		ORDER BY 
			intr_id DESC	
	</select>
	
	<select id="getIntroById" resultType="com.proj.resumy.intro.domain.IntroDTO">
		SELECT 
			intr_id "id", 
			intr_title title, 
			intr_regdate regdate, 
			intr_public pub, 
			intr_finish fin, 
			mody_dtm modydate,
			mem_id mid
		FROM hr_introduction 
		WHERE intr_id=#{id}
	</select>
	
	
</mapper> 
















