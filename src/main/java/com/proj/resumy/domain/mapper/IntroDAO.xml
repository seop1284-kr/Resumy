<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.proj.resumy.domain.IntroDAO">
	
	<select id="select" resultType="com.proj.resumy.domain.IntroDTO">
		SELECT 
			intr_id "id", 
			intr_title title, 
			intr_regdate regdate, 
			intr_public pub, 
			intr_finish fin, 
			mody_dtm modydate,
			mem_id mid
		FROM 
			hr_introduction 
		ORDER BY 
			intr_id DESC	
	</select>
	
	
</mapper> 
















