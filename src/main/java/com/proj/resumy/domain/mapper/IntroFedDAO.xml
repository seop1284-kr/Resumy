<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.proj.resumy.fed.domain.IntroFedDAO">
	
	<select id="select" resultType="com.proj.resumy.fed.domain.IntroFedDTO">
		SELECT 
			fb_id "id",
			fb_userid userid, 
			fb_content content, 
			fb_regdate regdate, 
			intr_id iid
		FROM 
			intr_feedback 
		ORDER BY 
			fb_content DESC	
	</select>
	
	<!-- 특정 id의 자소서 피드백 가져오기 -->
	<select id="selectById" resultType="com.proj.resumy.fed.domain.IntroFedDTO">
		SELECT 
			fb_id "id",
			fb_userid userid, 
			fb_content content, 
			fb_regdate regdate, 
			intr_id iid
		FROM 
			intr_feedback 
		WHERE
			fb_id = #{id}
		ORDER BY 
			fb_content DESC	
	</select>
	
	
</mapper> 
















