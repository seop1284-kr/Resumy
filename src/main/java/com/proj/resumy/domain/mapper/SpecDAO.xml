<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.proj.resumy.spec.domain.SpecDAO">

	<select id="select" resultType="com.proj.resumy.spec.domain.SpecDAO">
		SELECT 
			spec_id "id", 
			spec_cat_cd cat, 
			spec_name name, 
			spec_area area,
			spec_major major,
			spec_university university,
			reg_dtm regdate,
			mody_dtm modydate,
			mem_id mid
		FROM 
			hr_spec_info
		ORDER BY 
			spec_id DESC;
	</select>
</mapper>